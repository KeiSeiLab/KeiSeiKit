---
title: phase-4-ci-wire
path: test-matrix/phase-4-ci-wire.md
dna_hash: sha256:0bc8e4d712df915b
language: markdown
size_loc: 87
generated: by-keidocs
---

# test-matrix/phase-4-ci-wire.md

## Public API

- `Phase 4 — CI wiring per cell (artifacts + failure policy)` — Each scaffolded cell gets exactly one CI job. Different paradigms have
- `4a — Per-type failure policy (preview)` — Emit a table in chat showing the default policy per `MATRIX` cell:
- `4b — Confirm CI jobs (AskUserQuestion multi-select)` — ```json
- `4c — Write the workflow file(s)` — Based on `CI` from Phase 1:
- `4d — Artifact discipline` — Every job uploads one artifact directory, never loose files:
- `Verify-criterion` — - `CI_JOBS` has ≥ 1 entry (else redirect to local-only Makefile path).

## Related

- parent: `test-matrix`

## Discussion

<div id="keicomments-mount" data-page=""></div>
<script type="module" src="/keicomments.js"></script>
