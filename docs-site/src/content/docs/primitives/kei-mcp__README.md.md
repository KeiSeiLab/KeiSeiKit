---
title: README
path: kei-mcp/README.md
dna_hash: sha256:c73aedf626816e4c
language: markdown
size_loc: 106
generated: by-keidocs
---

# kei-mcp/README.md

## Public API

- `kei-mcp — Model Context Protocol server` — `kei-mcp` exposes the KeiSeiKit atom registry over the [Model Context
- `What you get` — - **Tools** — every atom in `_primitives/_rust/*/atoms/*.md` becomes one
- `Wire format` — JSON-RPC 2.0 over stdio, line-delimited (one request per line, one
- `Build` — ```sh
- `binary: target/release/kei-mcp` — ```
- `Configuration (env)` — | Variable | Default | What it does |
- `Register with Claude Code` — Add to `~/.claude/mcp_servers.json`:
- `Register with Cline` — Edit Cline's `cline_mcp_settings.json` (open via `Cline: Edit MCP
- `Register with OpenClaw` — Add to `~/.openclaw/mcp.json`:
- `Manual smoke test` — ```sh

## Related

- parent: `kei-mcp`

## Discussion

<div id="keicomments-mount" data-page=""></div>
<script type="module" src="/keicomments.js"></script>
